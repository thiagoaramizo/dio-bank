import styled from "styled-components"

export const Footer = () => {
    return (
        <FooterWrapper>
            Footer
        </FooterWrapper>
    )
}

const FooterWrapper = styled.div`
    
`